[package]
name = "pylink-basic-example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
pylink = { path = "../.." }

# Exercise pylink as an ordinary dependency of an independent binary crate.
[workspace]
