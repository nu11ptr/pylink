[env]
# Cargo resolves this relative to the directory containing .cargo, making the
# config portable across machines. Run Cargo from examples/pyo3-app.
PYO3_CONFIG_FILE = { value = "pyo3-config.txt", relative = true }
