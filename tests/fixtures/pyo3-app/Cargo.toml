[package]
name = "pylink-pyo3-smoke-test"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pylink = { path = "../../.." }
pyo3 = { version = "0.29.2", default-features = false }

# CI test application: exercise PyO3 as an ordinary downstream dependency.
[workspace]
